# Project Plan

## Project Name
Space Invaders React Game

## Planning Phase

1. **Planning Phase (Turn 1)**
   - Call `filesystem_write` to create `PROJECT_PLAN.md` and `ARCHITECTURE.md`.
   - Define: `Project Name: Space Invaders React Game`.
   - STOP. Wait for User Approval.

## Execution Phase

1. **Execution Phase (Turn 2+)**
   - Delegate INDIVIDUAL COMPONENTS (e.g., 'Make the PlayerShip component').
   - DO NOT use header names like 'Component Delegation' as agent IDs.
   - Use `delegate_to_agent` for ONE component at a time.

## Final Review Phase

1. **Final Review Phase**
   - Once all components are built, delegate a final 'Consistency & Integration' task to the appropriate developer to ensure imports and logic connect correctly.