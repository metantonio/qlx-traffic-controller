Project Name: Space Invaders React Game

# Project Plan

1. **Planning (Turn 1)**
   - Call `filesystem_write` to create `PROJECT_PLAN.md` (detailed task breakdown) and `ARCHITECTURE.md`.
   - Define the project name clearly in your first line: `Project Name: Space Invaders React Game`.
   - STOP. Wait for User Approval.

2. **Component Delegation (Turn 2+)**
   - DO NOT delegate the entire app. Delegate INDIVIDUAL COMPONENTS (e.g., 'Make the PlayerShip component', 'Make the GameLoop logic').
   - Call `delegate_to_agent` for ONE component at a time.